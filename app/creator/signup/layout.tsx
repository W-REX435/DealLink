import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Join as a Creator | DealLink',
  description:
    'Join the DealLink creator network free. Get matched with brands in your niche and turn your audience into sponsorship revenue.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
